# Local development database (used by docker-compose and Spring Boot dev profile).
# Copy to .env and adjust if needed. Do NOT commit .env with real secrets.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=lumbral_dev
DB_USER=lumbral
DB_PASSWORD=lumbral
